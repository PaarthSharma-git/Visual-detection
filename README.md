# Real-Time Visual Analysis
YOLOv8 tracks fire growth (px/sec). ESP8266+DHT11 verifies heat spikes to stop false alarms.

**Run:**
`pip install ultralytics opencv-python pyserial`
Plug in ESP8266, run `python main.py`.
